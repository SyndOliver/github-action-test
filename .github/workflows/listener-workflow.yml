name: Read Logs Workflow

on:
  workflow_run:
    workflows: ["Main Workflow - Run GitHub CLI and Save Logs"]
    types:
      - completed

jobs:
  read-logs:
    runs-on: ubuntu-latest
    steps:
      - name: Download logs from previous workflow
        uses: actions/download-artifact@v4
        with:
          name: gh-cli-logs

      - name: Display logs
        run: |
          echo "=== Logs from previous workflow ==="
          cat gh_log.txt
